require "spec_helper"

RSpec.describe TheBullet::Generator do
  it "has a version number" do
    expect(TheBullet::Generator::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
